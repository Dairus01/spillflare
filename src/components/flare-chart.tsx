"use client";
import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
export function FlareTrend({ data }: { data: Array<{ month: string; value: number }> }) { return <div style={{ width: "100%", height: 270 }}><ResponsiveContainer><AreaChart data={data}><defs><linearGradient id="flareFill" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#d75c23" stopOpacity={.42}/><stop offset="95%" stopColor="#d75c23" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="var(--border)" vertical={false}/><XAxis dataKey="month" tick={{ fill: "var(--muted)", fontSize: 10 }} tickLine={false} axisLine={false}/><YAxis tick={{ fill: "var(--muted)", fontSize: 10 }} tickLine={false} axisLine={false} tickFormatter={(value) => `${(value/1_000_000).toFixed(0)}M`}/><Tooltip formatter={(value) => `${new Intl.NumberFormat("en-NG").format(Number(value))} mscf`} contentStyle={{ background: "var(--surface)", border: "1px solid var(--border)", borderRadius: 8 }}/><Area dataKey="value" type="monotone" stroke="#d75c23" strokeWidth={2} fill="url(#flareFill)" isAnimationActive={false} /></AreaChart></ResponsiveContainer></div>; }
export function FlareBars({ data }: { data: Array<{ name: string; value: number }> }) { return <div style={{ width: "100%", height: 310 }}><ResponsiveContainer><BarChart data={data} layout="vertical" margin={{ left: 25 }}><CartesianGrid stroke="var(--border)" horizontal={false}/><XAxis type="number" hide/><YAxis dataKey="name" type="category" width={90} tick={{ fill: "var(--slate)", fontSize: 11 }} tickLine={false} axisLine={false}/><Tooltip formatter={(value) => `${new Intl.NumberFormat("en-NG").format(Number(value))} mscf`} contentStyle={{ background: "var(--surface)", border: "1px solid var(--border)", borderRadius: 8 }}/><Bar dataKey="value" fill="#147e78" radius={[0,4,4,0]} isAnimationActive={false} /></BarChart></ResponsiveContainer></div>; }
