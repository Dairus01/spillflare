import type {Metadata} from "next";
import Link from "next/link";
import { CheckCircle2, ExternalLink } from "lucide-react";
import { DataNote, SourceRail } from "@/components/ui";
import { getMetadata } from "@/lib/data";
import { formatDate, formatNumber } from "@/lib/format";
export const metadata:Metadata={title:"Data & methods",alternates:{canonical:"/data-and-methods"}};
export default async function MethodsPage(){const metadata=await getMetadata();const sourceRows=Object.entries(metadata.sources);return <><section className="page-hero"><div className="container"><span className="eyebrow">Transparency</span><h1>Data, definitions & methods</h1><p>What the product reads, what it calculates, what it does not claim and how missing values are handled.</p></div></section><SourceRail label={`${sourceRows.filter(([,s])=>s.status==="healthy").length}/${sourceRows.length} source endpoints healthy at snapshot`} retrieved={metadata.retrievedAt}/><div className="container page-pad"><div className="prose"><h2>The two core datasets</h2><p>Oil spill records come from the NOSDRA Oil Spill Monitor. The two published spill URLs return the same dataset in the captured snapshot, so records are deduplicated rather than counted twice.</p><p>Gas flare values and geographic layers come from the Nigeria Gas Flare Tracker. State, LGA, cluster, block, company and onshore/offshore aggregations are presented independently.</p><p>For reuse and attribution guidance, including the distinction between SpillFlare-created material and underlying source records, read the <Link href="/data-license">data license and reuse policy</Link>.</p><h2>Time means two different things</h2><p><strong>Observation date</strong> is the incident date or flare month in a source row. <strong>Retrieval date</strong> is when this product fetched the endpoint. A successful fetch today does not make an older observation current.</p><h2>Missing values</h2><p>A missing spill quantity is displayed as “not supplied,” because the exact quantity cannot always be ascertained. It is never converted to zero.</p><p>For gas flaring, a covered state or LGA with no row in a successfully retrieved period is described as “no flaring detected by the tracker for this period.” This wording fits both non-producing areas and areas where the tracker did not detect flaring; it does not make a broader geological claim.</p><h2>Offshore locations</h2><p>Coordinates outside Nigeria&apos;s land boundary can still be valid offshore incidents or flare sites. The map is designed to show the adjacent Gulf of Guinea rather than rejecting such records.</p><h2>Block name matching</h2><p>The flare time series and polygon layer sometimes use different names. A historical prospecting licence may later become a mining lease, while polygons may use lettered subdivisions or field names. We show verified lineage, but do not pretend those labels define identical boundaries. Unconfirmed joins are labelled <strong>location not matched</strong>.</p><h2>Company history</h2><p>Company flare data is included through October 2020—the final supplied month. It remains useful historically, but is never presented as a current company ranking.</p><h2>Population layer</h2><p>Where shown, the label is <strong>estimated population living within 2 km of flare locations</strong> and the value is reproduced from the tracker layer. The product does not convert it into an “affected population” estimate.</p><h2>What this product does not do</h2><ul><li>No predictive model, risk score or AI-generated environmental conclusion.</li><li>No penalties, emissions conversions or health-impact calculations without a verified source and method.</li><li>No authentication, private workspaces or user-uploaded evidence.</li></ul></div><section style={{marginTop:60}}><h2>Source health snapshot</h2><div className="table-wrap"><table><thead><tr><th>Source</th><th>Status</th><th>Rows/features</th><th>Latest observation</th><th>Endpoint</th></tr></thead><tbody>{sourceRows.map(([name,source])=><tr key={name}><td><strong>{name}</strong></td><td><span className="status"><CheckCircle2 size={13}/>{source.status}</span></td><td>{formatNumber(source.count)}</td><td>{source.latestObservation?formatDate(source.latestObservation,{month:"short",year:"numeric"}):"Geographic layer"}</td><td><a className="text-link" href={source.url} target="_blank" rel="noreferrer">Open <ExternalLink size={13}/></a></td></tr>)}</tbody></table></div></section><DataNote><strong>Snapshot fallback:</strong> If a live source temporarily fails, the product can keep serving the last validated snapshot and marks the source as degraded rather than silently returning an empty dataset.</DataNote></div></>}
